#include "types.h"
#include "stat.h"
#include "user.h"

int
main(int argc, char *argv[])
{
/*    char *buf = "Hello xv6!";
    int ret_val;
    ret_val = myfunction(buf);
    printf(1,"Return value : 0x%x\n", ret_val);
    exit();*/
/*    __asm__("int $128");
    exit();*/
    /*int pid = fork();
    while(1){
	if(pid > 0) printf(1,"Parent\n");
	else printf(1,"Child\n");
	yield();
    }*/
    exit();
}
